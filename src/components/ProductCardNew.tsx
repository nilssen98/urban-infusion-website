
interface Props {
    title?: string;
    price?: number;
    image_url?: string;
}

ProductCardNew.defaultProps = {
    title: '',
};

export default function ProductCardNew(props: Props) {
    return (
        <>
        </>
    );
}
